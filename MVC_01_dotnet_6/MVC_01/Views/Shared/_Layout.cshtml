<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NETCore Application</title>
   
    <link href="~/libs/bootstrap/css/bootstrap.css" rel="stylesheet" />
    
    <script src="~/libs/bootstrap/js/bootstrap.js"></script>
    <style>
        body {
            padding-top: 70px;
        }

        .body-content {
            min-height: calc(100vh - 150px);
        }
    </style>
    @await RenderSectionAsync("styles", false)


</head>
<body>
    <nav class="navbar navbar-dark bg-success fixed-top navbar-expand-md ">
        <div class="container">
            @Html.ActionLink("Class-01", "Index", "Home", null, new { @class = "navbar-brand text-uppercase" })
            <div class="navbar-collapse collapse">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        @Html.ActionLink("Home", "Index", "Home", null, new { @class = "nav-link" })
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Brands", "Index", "Brands", null, new { @class = "nav-link" })
                    </li>                    
                </ul>
            </div>
        </div>
    </nav>

    <div class="container-fluid body-content">
        @RenderBody()
    </div>
    <div class="container-fluid">
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - My ASP.NET Application</p>
        </footer>
    </div>
   


    @await RenderSectionAsync("scripts", false)
</body>
</html>